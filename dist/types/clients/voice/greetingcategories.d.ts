export class GreetingCategories extends Client {
    constructor(options: any);
    jsonAPINames: string[];
    list(): Promise<module>;
    show(greetingCategoryID: any): Promise<module>;
}
import { Client } from "../client";
